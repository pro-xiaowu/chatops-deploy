apiVersion: v1
kind: Service
metadata:
  name: chatops-deploy
  namespace: chatops-deploy
spec:
  selector:
    app.kubernetes.io/name: chatops-deploy
  ports:
    - name: http
      port: 80
      targetPort: http
