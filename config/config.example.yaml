mode: all

http:
  addr: ":8080"
  shutdown_timeout: 15s

database:
  # Set CHATOPS_DATABASE_URL with the database connection URL.
  max_open_conns: 20
  max_idle_conns: 5
  conn_max_lifetime: 30m

log:
  level: info
  development: false
